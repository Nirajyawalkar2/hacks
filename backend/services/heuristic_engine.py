"""Explainable heuristic phishing detection engine (placeholder)."""


def analyze_message(text: str) -> dict:
    """Return heuristic analysis. Full rules engine added in a later task."""
    return {
        "risk_score": 0.0,
        "risk_level": "low",
        "signals": [],
        "input_length": len(text),
    }
